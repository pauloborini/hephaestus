# INDEX: SHARED

## Required

1. `project-rules/rules/domain_rules.md`
2. `project-rules/rules/operational_rules.md`

## On trigger

- `architecture_rules.md`: the shared item crosses layers or packages.
- `error_handling_rules.md`: error helper or operation result.
- `security_rules.md`: sensitive data in a VO/enum/helper.

Load every triggered rule before the first edit, in batches of up to two.

## References

- `domain_examples.md`: VO/enum or shared-model pattern.
- Omit this section when there is no actionable reference.
