# REFERENCE: API CONTRACT

API contract reference for on-trigger consultation.

## What to fill in during synthesis

- relevant endpoints, payloads, and pagination;
- error codes and standard responses;
- pointer to `project-rules/contracts/*.openapi.json` when present;
- known divergences between contract and implementation, if any.
