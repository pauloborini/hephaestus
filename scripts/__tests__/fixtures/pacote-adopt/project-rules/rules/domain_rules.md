# Regras de domínio

Toda transação de pagamento deve ser idempotente e auditável.

## Decisões de produto vigentes

Normas de produto com efeito observável pelo usuário final vivem como decisão em `_app-vault/docs/decisions/`; `project-rules/` referencia o ID, nunca copia o valor (D18).

- Ideias — ver DEC-001 em `_app-vault/docs/decisions/ideias.md`.
- Limite de consultas — ver DEC-002 em `_app-vault/docs/decisions/produto.md`.
