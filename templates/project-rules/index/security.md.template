# INDEX: SECURITY

## Required

1. `project-rules/rules/security_rules.md`
2. `project-rules/rules/operational_rules.md`

## On trigger

- `auth_rules.md`: auth, session, role, permission, or redirect.
- `domain_rules.md`: sensitive data crossing DTO/Entity.
- `error_handling_rules.md`: internal-detail leak in errors.

Load every triggered rule before the first edit, in batches of up to two.

## References

Omit this section when there is no actionable reference.
